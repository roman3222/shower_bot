# ⚡ Быстрый старт

## 5 минут до запуска бота

### Шаг 1: Получите токен (2 минуты)

1. Откройте Telegram
2. Найдите **@BotFather**
3. Отправьте `/newbot`
4. Введите имя: `Showe Car Bot`
5. Введите username: `showe_car_bot` (или любой другой)
6. Скопируйте полученный токен

### Шаг 2: Установите зависимости (2 минуты)

```bash
cd /home/roman/projects/showe_car
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### Шаг 3: Настройте токен (1 минута)

Отредактируйте файл `.env`:

```bash
nano .env
```

Замените:
```
BOT_TOKEN=YOUR_BOT_TOKEN_HERE
```

На ваш токен:
```
BOT_TOKEN=123456789:ABCdefGHIjklmnoPQRstuvWXYZ
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### Шаг 4: Запустите бота (1 минута)

```bash
python bot.py
```

Вы должны увидеть:
```
INFO:telegram.ext._application:Application started
```

### Готово! 🎉

Теперь откройте Telegram и найдите вашего бота по username.

---

## Тестирование функциональности

### Тест 1: Запись на услугу
1. Нажмите `/start`
2. Выберите "Базовая мойка"
3. Выберите дату
4. Выберите время
5. Введите номер: `+79991234567`
6. Подтвердите запись

### Тест 2: Просмотр записей
1. Нажмите `/start`
2. Нажмите "📋 Мои записи"
3. Должна отобразиться ваша запись

### Тест 3: Отмена записи
1. В меню "Мои записи"
2. Нажмите "❌ Отменить запись"
3. Запись должна исчезнуть

---

## Структура проекта

```
showe_car/
├── bot.py              ← Основной файл (запускайте его)
├── config.py           ← Настройки
├── database.py         ← Работа с БД
├── requirements.txt    ← Зависимости
├── .env               ← Токен (отредактируйте!)
├── README.md          ← Полная документация
├── API.md             ← API документация
├── DEPLOYMENT.md      ← Развертывание на сервер
├── FEATURES.md        ← Все возможности
├── examples.py        ← Примеры кода
└── tests.py           ← Тесты
```

---

## Основные команды

```bash
# Запуск бота
python bot.py

# Запуск тестов
python -m unittest tests.py

# Просмотр примеров
python examples.py

# Активация окружения
source venv/bin/activate

# Деактивация окружения
deactivate
```

---

## Часто задаваемые вопросы

### Q: Бот не запускается
**A:** Проверьте:
1. Установлены ли зависимости: `pip install -r requirements.txt`
2. Правильный ли токен в `.env`
3. Интернет соединение

### Q: Как изменить время работы?
**A:** Отредактируйте `config.py`:
```python
WORKING_HOURS = {
    'start': 8,      # Начало в 8:00
    'end': 20,       # Конец в 20:00
    'interval': 1    # Интервал 1 час
}
```

### Q: Как добавить новую услугу?
**A:** В `bot.py` найдите `self.services` и добавьте:
```python
'new_service': {'name': 'Новая услуга', 'price': 1500}
```

### Q: Где хранятся данные?
**A:** В файле `carwash_bot.db` (SQLite база данных)

### Q: Как сделать резервную копию?
**A:** Просто скопируйте файл `carwash_bot.db`

---

## Следующие шаги

1. **Прочитайте документацию**
   - `README.md` - Полный обзор
   - `API.md` - Описание функций
   - `FEATURES.md` - Все возможности

2. **Настройте под себя**
   - Измените услуги в `bot.py`
   - Измените время работы в `config.py`
   - Добавьте выходные дни

3. **Разверните на сервер**
   - Следуйте инструкциям в `DEPLOYMENT.md`
   - Используйте systemd или Docker

4. **Добавьте функции**
   - Платежи
   - Уведомления
   - Админ-панель

---

## Поддержка

Если что-то не работает:
1. Проверьте логи: `python bot.py` (смотрите вывод)
2. Прочитайте документацию
3. Проверьте примеры в `examples.py`
4. Запустите тесты: `python -m unittest tests.py`

---

## Лицензия

MIT - используйте своб��дно!

---

**Удачи с вашим ботом! 🚀**
