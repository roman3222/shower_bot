#!/bin/bash

# Скрипт для запуска Telegram бота для автомойки

echo "🚗 Запуск Telegram бота для записи на автомойку..."
echo ""

# Проверяем наличие виртуального окружения
if [ ! -d "venv" ]; then
    echo "📦 Создание виртуального окружения..."
    python3 -m venv venv
fi

# Активируем виртуальное окружение
source venv/bin/activate

# Проверяем наличие зависимостей
if ! python -c "import telegram" 2>/dev/null; then
    echo "📥 Установка зависимостей..."
    pip install -r requirements.txt
fi

# Проверяем наличие токена
if grep -q "YOUR_BOT_TOKEN_HERE" .env; then
    echo ""
    echo "⚠️  ВНИМАНИЕ: Токен бота не установлен!"
    echo "1. Откройте файл .env"
    echo "2. Замените YOUR_BOT_TOKEN_HERE на в��ш токен от @BotFather"
    echo "3. Сохраните файл"
    echo ""
    exit 1
fi

echo "✅ Все готово! Запускаем бота..."
echo ""

# Запускаем бота
python bot.py
