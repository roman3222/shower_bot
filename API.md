# 📚 API Документация

## Структура проекта

### `bot.py` - Основной файл бота
Содержит класс `CarWashBot` с обработчиками всех команд и callback'ов.

#### Основные методы:

**`start(update, context)`**
- Обработчик команды `/start`
- Показывает главное меню с выбором услуг
- Добавляет пользователя в БД

**`select_service(update, context)`**
- Обработчик выбора услуги
- Получает список доступных дат
- Показывает календарь для выбора

**`select_date(update, context)`**
- Обработчик выбора даты
- Получает доступное время для выбранной даты
- Показывает список времени

**`select_time(update, context)`**
- Обработчик выбора времени
- Запрашивает номер ��елефона

**`enter_phone(update, context)`**
- Обработчик ввода номера телефона
- Валидирует номер
- Показывает подтверждение записи

**`confirm_booking(update, context)`**
- Обработчик подтверждения записи
- Добавляет запись в БД
- Показывает успешное сообщение

**`show_my_bookings(query, context)`**
- Показывает все активные записи пользователя
- Позволяет отменить запись

**`cancel_booking_handler(update, context)`**
- Обработчик отмены записи
- Обновляет статус в БД

---

### `database.py` - Работа с БД

Класс `Database` управляет всеми операциями с SQLite БД.

#### Основные методы:

**`init_db()`**
- Инициализирует БД
- Создает таблицы `users` и `bookings`

**`add_user(user_id, username, first_name)`**
- Добавляет или обновляет пользователя
- Параметры:
  - `user_id` (int): ID пользователя Telegram
  - `username` (str): Имя пользователя
  - `first_name` (str): Имя

**`update_user_phone(user_id, phone)`**
- Обновляет номер телефона пользователя
- Параметры:
  - `user_id` (int): ID пользователя
  - `phone` (str): Номер телефона в формате +7XXXXXXXXXX

**`get_available_dates()`**
- Возвращает список доступных дат
- Исключает понедельники (выходной)
- Возвращает даты на 14 дней вперед
- Возвращает: `list[datetime.date]`

**`get_available_times(date_str)`**
- Получает доступное время для конкретной даты
- Параметры:
  - `date_str` (str): Дата в формате 'YYYY-MM-DD'
- Возвращает: `list[dict]` с ключами 'time' и 'available'

**`add_booking(user_id, booking_date, booking_time, service, phone)`**
- Добавляет новую запись
- Параметры:
  - `user_id` (int): ID пользователя
  - `booking_date` (str): Дата в формате 'YYYY-MM-DD'
  - `booking_time` (str): Время в формате 'HH:MM'
  - `service` (str): Название услуги
  - `phone` (str): Номер телефона
- Возвращает: `bool` (успех/ошибка)

**`get_user_bookings(user_id)`**
- Получает все активные записи пользователя
- Параметры:
  - `user_id` (int): ID пользователя
- Возвращает: `list[sqlite3.Row]`

**`cancel_booking(booking_id, user_id)`**
- Отменяет запись (меняет статус на 'cancelled')
- Параметры:
  - `booking_id` (int): ID записи
  - `user_id` (int): ID пользователя

---

### `config.py` - Конфигурация

Содержит все настройки бота:

```python
BOT_TOKEN              # Токен Telegram бота
DB_PATH               # Путь к БД
WORKING_HOURS         # Время работы (9:00 - 19:00)
DAYS_AHEAD            # Количество дней для записи (14)
MAX_BOOKINGS_PER_SLOT # Максимум записей на слот (3)
```

---

## Схема БД

### Таблица `users`
```sql
CREATE TABLE users (
    user_id INTEGER PRIMARY KEY,
    username TEXT,
    first_name TEXT,
    phone TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

### Таблица `bookings`
```sql
CREATE TABLE bookings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    booking_date DATE NOT NULL,
    booking_time TEXT NOT NULL,
    service TEXT NOT NULL,
    phone TEXT NOT NULL,
    status TEXT DEFAULT 'active',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(user_id),
    UNIQUE(booking_date, booking_time)
)
```

---

## Поток работы бота

```
START
  ↓
Выбор услуги
  ↓
Выбор даты
  ↓
Выбор времени
  ↓
Ввод номера телефона
  ↓
Подтверждение записи
  ↓
Запись добавлена в БД
  ↓
END
```

---

## Примеры использования

### Добавление записи
```python
from database import Database

db = Database()
success = db.add_booking(
    user_id=123456789,
    booking_date='2024-01-15',
    booking_time='10:00',
    service='Стандартная мойка',
    phone='+79991234567'
)
```

### Получение доступных дат
```python
available_dates = db.get_available_dates()
for date in available_dates:
    print(date.strftime('%d.%m.%Y'))
```

### Получение доступного времени
```python
times = db.get_available_times('2024-01-15')
for slot in times:
    print(f"{slot['time']} - {slot['available']} мест")
```

### Получение записей пользователя
```python
bookings = db.get_user_bookings(123456789)
for booking in bookings:
    print(f"{booking['booking_date']} {booking['booking_time']}")
```

---

## Обработка ошибок

### Валидация номера телефона
Номер должен быть в формате: `+7XXXXXXXXXX`
- Начинается с `+7`
- Содержит ровно 10 цифр после `+7`

### Проверка доступности времени
- Максимум 3 записи на один слот времени
- Если слот занят, показывается ошибка

### Дублирование записей
- Невозможно создать две записи на одно время
- ��Д имеет UNIQUE constraint на (booking_date, booking_time)

---

## Расширение функциональности

### Добавление новой услуги
Отредактируйте словарь `services` в классе `CarWashBot`:

```python
self.services = {
    'basic': {'name': 'Базовая мойка', 'price': 500},
    'new_service': {'name': 'Новая услуга', 'price': 1500}
}
```

### Изменение времени работы
Отредактируйте `config.py`:

```python
WORKING_HOURS = {
    'start': 8,      # Начало в 8:00
    'end': 20,       # Конец в 20:00
    'interval': 1    # Интервал 1 час
}
```

### Добавление выходных дней
В методе `get_available_dates()` добавьте проверку:

```python
# Пропускаем понедельники и вторники
if date.weekday() in [0, 1]:
    continue
```

---

## Тестирование

Запустите тесты:
```bash
python -m unittest tests.py
```

Или конк��етный тест:
```bash
python -m unittest tests.TestDatabase.test_add_booking
```
