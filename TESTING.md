# 🧪 Руководство по тестированию

## Запуск тестов

### Все тесты
```bash
python -m unittest tests.py
```

### Конкретный тест
```bash
python -m unittest tests.TestDatabase.test_add_booking
```

### С подробным выводом
```bash
python -m unittest tests.py -v
```

---

## Тесты БД

### test_add_user
Проверяет добавление пользователя в БД.

```python
def test_add_user(self):
    self.db.add_user(123, 'testuser', 'Test')
    # Проверяет, что пользователь добавлен
```

### test_update_phone
Проверяет обновление номера телефона.

```python
def test_update_phone(self):
    self.db.add_user(123, 'testuser', 'Test')
    self.db.update_user_phone(123, '+79991234567')
    # Проверяет, что номер обновлен
```

### test_add_booking
Проверяет добавление записи.

```python
def test_add_booking(self):
    success = self.db.add_booking(...)
    # Проверяет, что запись добавлена
```

### test_duplicate_booking
Проверяет предотвращение дублирования записей.

```python
def test_duplicate_booking(self):
    # Первая запись должна пройти
    success1 = self.db.add_booking(...)
    # Вторая запись на то же время должна не пройти
    success2 = self.db.add_booking(...)
```

### test_get_available_times
Проверяет получение доступного времени.

```python
def test_get_available_times(self):
    times = self.db.get_available_times('2024-01-15')
    # Проверяет, что есть 10 слотов
```

### test_get_user_bookings
Проверяет получение записей пользователя.

```python
def test_get_user_bookings(self):
    # Добавляет несколько записей
    # Проверяет, что все записи получены
```

### test_cancel_booking
Проверяет отмену записи.

```python
def test_cancel_booking(self):
    # Добавляет запись
    # Отменяет запись
    # Проверяет, что запись отменена
```

---

## Тесты валидации

### test_valid_phone
Проверяет валидные номера телефонов.

```python
def test_valid_phone(self):
    self.assertTrue(CarWashBot.validate_phone('+79991234567'))
    self.assertTrue(CarWashBot.validate_phone('+71234567890'))
```

### test_invalid_phone
Проверяет невалидные номера телефонов.

```python
def test_invalid_phone(self):
    self.assertFalse(CarWashBot.validate_phone('79991234567'))  # Без +
    self.assertFalse(CarWashBot.validate_phone('+7999123456'))   # Короткий
```

---

## Ручное тестирование

### Сценарий 1: Полная запись

1. Запустите бота: `python bot.py`
2. Откройте Telegram
3. Найдите вашего бота
4. Нажмите `/start`
5. Выберите "Базовая мойка"
6. Выберите дату (например, завтра)
7. Выберите время (например, 10:00)
8. Введите номер: `+79991234567`
9. Нажмите "✅ Подтвердить"
10. Проверьте, что запись добавлена

**Ожидаемый результат:** Сообщение "🎉 Спасибо! Ваша запись подтверждена!"

### Сценарий 2: Просмотр записей

1. Нажмите `/start`
2. Нажмите "📋 Мои записи"
3. Должна отобразиться ваша запись

**Ожидаемый результат:** Список всех ваших записей

### Сценарий 3: Отмена записи

1. В меню "Мои записи"
2. Нажмите "❌ Отменить запись #1"
3. Запись должна исчезнуть

**Ожидаемый результат:** Сообщение "✅ Запись отменена."

### Сценарий 4: Невалидный номер телефона

1. Выберите услугу, дату и время
2. Введите невалидный номер: `123456789`
3. Должна появиться ошибка

**Ожидаемый результат:** Сообщение об ошибке и повторный запрос

### Сценарий 5: Отмена операции

1. Нажмите `/start`
2. Нажмите "❌ Отмена"
3. Диалог должен завершиться

**Ожидаемый результат:** Сообщение "❌ Операция отменена."

### Сценарий 6: Возврат назад

1. Выберите услугу
2. Нажмите "⬅️ Назад"
3. Должны вернуться к выбору услуги

**Ожидаемый результат:** Меню с услугами

### Сценарий 7: Максимум записей на слот

1. Создайте 3 записи на одно время (используя разные аккаунты)
2. Попробуйте создать 4-ю запись
3. Должна появиться ошибка

**Ожидаемый результат:** Сообщение об ошибке

### Сценарий 8: Выходной день (понедельник)

1. Нажмите `/start`
2. Выберите услугу
3. Проверьте, что понедельники не отображаются

**Ожидаемый результат:** Понедельники отсутствуют в календаре

---

## Проверка БД

### Просмотр всех записей

```bash
sqlite3 carwash_bot.db
sqlite> SELECT * FROM bookings;
```

### Просмотр всех пользователей

```bash
sqlite> SELECT * FROM users;
```

### Проверка целостности БД

```bash
sqlite> PRAGMA integrity_check;
```

### Выход из sqlite3

```bash
sqlite> .quit
```

---

## Проверка логов

### Запуск с логированием

```bash
python bot.py 2>&1 | tee bot.log
```

### Просмотр логов

```bash
tail -f bot.log
```

### Поиск ошибок в логах

```bash
grep ERROR bot.log
```

---

## Тестирование производительности

### Создание большого количества записей

```python
from database import Database
from datetime import datetime, timedelta

db = Database()

# Создаем 100 записей
for i in range(100):
    date = (datetime.now().date() + timedelta(days=i % 14 + 1)).strftime('%Y-%m-%d')
    time = f"{9 + (i % 10):02d}:00"
    
    db.add_booking(
        user_id=i,
        booking_date=date,
        booking_time=time,
        service='Базовая мойка',
        phone=f'+7999123{i:04d}'
    )

print("✅ 100 записей создано")
```

### Измерение времени запроса

```python
import time
from database import Database

db = Database()

start = time.time()
dates = db.get_available_dates()
end = time.time()

print(f"Время получения дат: {end - start:.4f} сек")
```

---

## Чек-лист перед развертыванием

- [ ] Все тесты проходят: `python -m unittest tests.py`
- [ ] Бот запускается без ошибок: `python bot.py`
- [ ] Токен ��становлен в `.env`
- [ ] БД создается автоматически
- [ ] Можно создать запись
- [ ] Можно просмотреть записи
- [ ] Можно отменить запись
- [ ] Валидация номера телефона работает
- [ ] Логи записываются корректно
- [ ] Нет утечек памяти (проверить через `top`)

---

## Решение проблем при тестировании

### Проблема: Тесты не запускаются
**Решение:**
```bash
pip install -r requirements.txt
python -m unittest tests.py -v
```

### Проблема: БД заблокирована
**Решение:**
```bash
rm carwash_bot.db
python bot.py
```

### Проблема: Бот не отвечает
**Решение:**
1. Проверьте токен в `.env`
2. Проверьте интернет соединение
3. Перезагрузите бота: `Ctrl+C` и `python bot.py`

### Проблема: Ошибка при добавлении записи
**Решение:**
1. Проверьте, что время еще не занято
2. Проверьте формат даты (YYYY-MM-DD)
3. Проверьте формат времени (HH:MM)

---

## Отчет о тестировании

Используйте этот шаблон для документирования результатов:

```
Дата тестирования: 2024-01-15
Версия бота: 1.0.0
Тестировщик: Иван

Результаты:
- Запуск бота: ✅ PASS
- Добавление записи: ✅ PASS
- Просмотр записей: ✅ PASS
- Отмена записи: ✅ PASS
- Валидация номера: ✅ PASS
- Обработка ошибок: ✅ PASS

Проблемы:
- Нет

Рекомендации:
- Добавить уведомления
- Добавить платежи
```

---

## Автоматизированное тестирование

### Запуск тестов при каждом коммите

Создайте файл `.git/hooks/pre-commit`:

```bash
#!/bin/bash
python -m unittest tests.py
if [ $? -ne 0 ]; then
    echo "Тесты не прошли!"
    exit 1
fi
```

Сделайте файл исполняемым:

```bash
chmod +x .git/hooks/pre-commit
```

---

## Непрерывное тестирование

### GitHub Actions

Создайте файл `.github/workflows/tests.yml`:

```yaml
name: Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: 3.11
      - run: pip install -r requirements.txt
      - run: python -m unittest tests.py
```

---

**Удачи с тестированием! 🧪**
